# Claude API
ANTHROPIC_API_KEY=

# FRED (Federal Reserve Economic Data — free at fred.stlouisfed.org/docs/api)
FRED_API_KEY=

# Upstash Redis (add the Upstash Redis integration from Vercel Marketplace)
# Vercel will auto-populate these when you connect the integration
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: lock the cron endpoint so only Vercel can trigger it
CRON_SECRET=
