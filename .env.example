# Claude API
ANTHROPIC_API_KEY=

# FRED (Federal Reserve Economic Data — free at fred.stlouisfed.org/docs/api)
FRED_API_KEY=

# Vercel KV (auto-populated by Vercel when you add KV storage to the project)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: lock the cron endpoint so only Vercel can trigger it
CRON_SECRET=
